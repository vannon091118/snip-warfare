extends Objekt_Basis
class_name Objekt_BodenObj
## Datenklasse für den baubaren Boden (Bauflächen-Knoten, keine Kachel).

func aus_katalog_eintrag(eintrag: Dictionary) -> void:
	super.aus_katalog_eintrag(eintrag)
