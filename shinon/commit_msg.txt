1. Du wolltest einen Verbindungs-Audit, der jede Doppelanbindung findet, und ich habe die Kabel gezogen, statt dir hübsche Versprechen zu drehen.
2. In kern_modifikator_registry.gd lebt jetzt genau eine geteilte Instanz der Modifikator-Daten, und in kern_modifikator_maschine.gd habe ich ihren zweiten privaten Cache ausgerissen, der dieselbe Datei still daneben noch einmal lud.
3. In einheit_manager.gd legt die Ernte-Kette ihren Absender mit bind am Status fest, sodass der Arbeitsschritt einer Einheit genau einmal bucht, statt den alten Sammel-Lauf über alle Einheiten mit einem zweiten Empfänger laufen zu lassen.
4. In pop_mood_maschine.gd ziehen Kälte und Hitze ihre Grenzen jetzt nur aus der Registry, und einheit_vital_status.gd urteilt über denselben Schwellwert, womit die zwei Sätze hart codierter Zahlen neben der Datenwahrheit gestorben sind.
5. Architektur.md trägt einen neuen Abschnitt über die geprüften Verbindungswege und benennt für Tick, Ernte, Wärme, Modifikatoren und Aktivierung, wer wann einmalig verbindet, damit kein Nächster wieder raten muss.
6. Der Preflight zählt 137 Klassen auf 154 Dateien mit null Befunden, und im Spiel bucht dieselbe Jagd, dieselbe Kälte und dieselbe Modifikator-Zeit wie zuvor, nur eben ohne Doppelleitung.
7. Bleiben tun die offenen Beobachtungspunkte: Die Werte kaelte und hitze in needs.json sind weiterhin lesende Beschreibung, und die Logik-Registry wartet weiter auf ihren Plugin-Verbraucher.
