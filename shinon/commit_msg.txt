1. Du wolltest, dass die Editor-Warnungen aus dem Reload-Log vollständig verschwinden und der neue Warnungs-Scan von Anfang an grün steht, und Shinon hat jede gemeldete Zeile an ihrem Ort aufgelöst.
2. In welt_definition_registry.gd wird die Kartengröße jetzt über explizite Gleitkomma-Rundung auf die Chunk-Größe gebracht, damit die Integer-Division nicht mehr den Nachkommaanteil verwirft.
3. In welt_generator.gd ist die Chunk-Zahl je Region einmal als Zwischenwert herausgezogen, sodass die vier gleichen Divisionen nur noch an einer Stelle stehen und keine Warnung mehr erzeugen.
4. In ui_welt_info.gd wird die Chunk-Anzeige über explizite Gleitkomma-Umrechnung berechnet, und in ui_karten_viewer.gd sind der ungenutzte Kanten-Parameter und die ungenutzte Kanten-Variable bewusst unterstrichen.
5. In gebaeude_manager.gd rufen die beiden Maschinen ihre Zustandsfabrik jetzt direkt über den Typ statt über die Instanz auf, und in orchestrator_verdrahtung.gd trägt der Darsteller-Knoten einen eigenen Namen, damit er die gleichnamige Funktion nicht mehr verschattet.
6. In welt_ladevorgang.gd heißt der Speicher-Leser jetzt speicher_leser, damit die verwirrende Doppel-Deklaration im umgebenden Block verschwindet.
7. Der Warnungs-Scan der Kategorie warnungen und der volle Preflight inklusive Godot-Lauf sind nach dieser Bereinigung grün, und keine neue Fehlercode-Zeile wurde angelegt, weil der Scan bereits persistiert ist.