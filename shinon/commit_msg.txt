1. Du wolltest nach Slice A und Slice B die letzten offenen Punkte G1 und G2 abarbeiten, also habe ich zuerst am heutigen Code bewiesen, dass die Objekt-Faktoren und die logik_id Verbindung wirklich noch ohne jeden Verbraucher waren.
2. Der rote Test in test_objekt_faktor_verdrahtung.py verlangte sechs Dinge auf einmal, und er schlug wie erwartet fehl, weil effektive_logik, effektiver_modifikator und der Objekt-Faktor tote Felder waren und die Kern_LogikRegistry kein einziges Mal aufgerufen wurde.
3. In core/logic/kern_logik_registry.gd wohnt jetzt eine geteilte Instanz nach dem bewährten Modifikator-Vorbild, und der Leser faktor_fuer liefert den Basisfaktor der Logik mit neutralem Rückfall 1.0 für unbekannte IDs, damit ein fehlender Eintrag nichts verlangsamt.
4. In world/logic/kategorie_objekt/objekt_basis.gd und world/logic/kategorie_tier/tier_basis.gd exponiert effektiver_faktor die einzige Faktor-Wahrheit als eigener Faktor mal dem Logik-Basisfaktor, und der öffentliche Leser tier_effektiver_faktor am Tier_Manager hält die Domänen-Grenze sauber.
5. In game/logic/kategorie_job/job_basis.gd nimmt ziel_faktor_setzen den Faktor des Ziels entgegen, und harvest_zeit_ticks teilt die Basiszeit durch ihn, sodass Faktor über 1 schneller arbeitet und unter 1 langsamer, alles über dieselbe eine Zeitformel.
6. In game/logic/kategorie_einheit/einheit_manager.gd und einheit_ziel_suche.gd setzen alle drei Job-Starten, direkte Vergabe, Queue-Start und Loop-Fortsetzung, den Faktor ihres Ziels, damit kein Startweg an der Naht vorbeischleicht.
7. In core/data/kern_logik.json bekam das Lagerfeuer mit waerme_quelle seinen echten Logik-Eintrag, weil es vorher still ins Leere verwies, und in tools/lauf_pruefung_welt.gd beweist die 29. Prüfung als Verhalten, dass 2.0 die Arbeit halbiert und 0.5 sie verdoppelt.
8. Da heute alle Katalog-Faktoren 1.0 tragen, ist das Spielverhalten identisch geblieben, der Lauf meldet ALLE PRUEFUNGEN GRUEN, python tools/preflight.py meldet PREFLIGHT OK, und ein herrenloses override.cfg des entfernten MCP-Addons wurde geparkt, weil es die Läufe mit toten Autoload-Pfaden störte.
9. Architektur.md und docs/Inventur_Stand.md führen G1 und G2 jetzt als geschlossen, damit keine alte Behauptung mehr von einer Plugin-Grenze ohne Verbraucher träumt.
