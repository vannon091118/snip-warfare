1. Du wolltest Slice A direkt umsetzen und danach Slice B, also habe ich zuerst die Objekt-Plugin-Naht nach dem bewährten script Muster aus Jobs und Needs errichtet.
2. Der rote Test in test_objekt_plugin_naht.py verlangte zunächst das script Feld für alle Registry-Objekte und die Naht in der Registry, und er schlug wie erwartet fehl, weil der Katalog nichts trug und die Zuordnung geschlossen war.
3. In world/data/element_katalog.json tragen jetzt alle zehn Objekte mit eigener Klasse ihr script Feld, von boden über baum und haus bis lagerfeuer, während Tier Einträge bewusst außen vor bleiben weil sie durch die Tier Registry laufen.
4. In world/logic/kategorie_objekt/objekt_registry_basis.gd lädt _objekt_klasse_fuer jetzt zuerst das script Feld mit Existenz- und Typprüfung, und world/logic/kategorie_welt/welt_registry.gd hält die alte Zuordnung nur noch als _zentrale_klasse_fuer Fallback vor.
5. In Architektur.md trägt die Welt-Objekte Zeile die neue Erweiterungsgrenze und in docs/Inventur_Stand.md ist Slice 0 vom offen auf erledigt umgesprungen, beide ohne alte Lüge.
6. Der Laufbeweis meldet ALLE PRUEFUNGEN GRUEN, Seed 12345 reproduziert dieselben 25 Objekte, und python tools/preflight.py bleibt grün, damit jedes neue Objekt künftig nur Katalog Eintrag plus SVG braucht.
