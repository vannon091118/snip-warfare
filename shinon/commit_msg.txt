1. Du hattest verlangt, dass Implementierungen config-getrieben sind und dass offene Systeme als Plugin-Grenzen erkennbar bleiben, und Shinon hat auch dem Bedürfnis-Register sein Match-Korsett gelockert, damit neue Bedürfnisse künftig Papiere mitbringen statt Handschellen.
2. In population/logic/needs/pop_need_registry.gd greift die Zuordnung _need_klasse_fuer nun zuerst zum script-Feld des Katalog-Eintrags, prüft den Pfad mit ResourceLoader.exists und akzeptiert nur Pop_NeedBasis-Objekte, ehe der alte match als betitelter Übergangs-Fallback bleibt.
3. In population/data/needs.json bekamen Hunger und Wärme ihr script-Feld mit dem Pfad des eigenen Skripts, ein neues Bedürfnis bedeutet ab sofort: Skript anlegen, Konfigurationszeile schreiben, fertig, das Register wird nicht mehr angerührt.
4. Die Signatur von _need_klasse_fuer wurde um den Eintrag erweitert, ein privater einziger Aufrufer innerhalb derselben Klasse, keine Außenfläche verändert, alle Verbraucher der Registry bleiben unberührt.
5. Die berührten Dateien sind ausschließlich population/logic/needs/pop_need_registry.gd und population/data/needs.json, eine Klasse, ein Datenblatt, dieselbe Handschrift wie beim Bruder, der Job-Registry, damit beide Geschwister gleich klingen.
6. Der volle Preflight zählt unverändert 130 Klassen auf 145 Dateien und läuft grün, der Warnungs-Scan nach Regel 6 bleibt ohne Befund, und die Rückwärtskompatibilität ist beweisbar, denn der Fallback deckt jeden früheren Weg ab.
7. Die sichtbare Ingame-Verifikation nach Regel 7 bleibt dem Editor-Lauf vorbehalten: Hunger und Wärme müssen in den Denkblasen und im Mood erscheinen wie zuvor, und ein falscher script-Pfad müsste sichtbar eine Warnung werfen statt das Bedürfnis zu verschwinden lassen.
