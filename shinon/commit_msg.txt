1. Der Nutzer verlangte Slices fuer die Warteschlange, deshalb trug ich als zweiten Wagen die Moebel-Abschlussarbeit, die der Parallel-Thread zurueckgelassen hatte, und die neue Ordnung der Klassen-Zuordnung, die mein eigener Schritt aus der Fassade gezogen hatte.

2. Die alten Umlaut-Dateien game/data/moebel.json und die fuenf svg unter world/assets/moebel verschwanden endgueltig aus dem Baum, der abschliessende Strich unter der doppelten Kodierung, die frueher sieben E015-Befunde erfand.

3. In world/logic/kategorie_objekt/objekt_moebel_platzierer.gd zog der neue Platzierer ein, der Moebel auf ihre Kacheln setzt, und in tools/lauf_moebel_beweis.gd liegt der Beweislauf dazu, der Platzierung sichtbar am Modell zeigt.

4. In world/logic/kategorie_welt/welt_registry_klassen_zuordnung.gd wohnt jetzt die Tabelle, die je Element-ID die konkrete Klasse nennt, und in welt_registry.gd fragt die Fassade nur noch dort nach, laedt ueber den Plugin-Naht-Pfad und warnt bildlich, wenn eine Zeile der Tabelle ohne Datei bleibt.

5. In world/logic/kategorie_objekt/moebel_registry.gd behaelt die Moebel-Logik ihren eigenen Fallback und ihre eigene Sicht, in ui/logic/kategorie_ui/ui_bau_panel.gd liest das Bau-Panel direkt bei der Fach-Registry statt an der Fassade zu klopfen.

6. In test_landschaft_objekte.py zog der Test der Fallbacks in sein neues Zuhause um, denn er prueft die Zuordnung, nicht mehr die Fassade, und alle 97 Pruefungen bestehen.

7. Der Preflight laeuft gruen, der Moebel-Thread ist damit vollstaendig geborgen, und auf der Karte stehen Tisch und Stuhl wieder da, wo der Spieler sie hinstellt.
