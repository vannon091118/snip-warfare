1. Du hattest verlangt, dass offene Systeme über eine Plugin-Architektur abgekoppelt werden und dass Implementierungen config-getrieben sind, und Shinon hat der Job-Registry ihr Match-Korsett gelockert, damit neue Berufe künftig nur noch Papiere brauchen und keine Schere am Code.
2. In game/logic/kategorie_job/job_registry.gd trägt die zentrale Zuordnung _job_erzeugen nun zuerst das script-Feld aus der Konfiguration, lädt das Skript mit load, prüft mit ResourceLoader.exists gegen Tippfehler-Halluzinationen und verweigert Objekte, die kein Job_Basis sind, ehe der alte match als ausdrücklich betitelter Übergangs-Fallback für Einträge ohne script-Feld bleibt.
3. In game/data/job_config.json bekamen alle sechs Berufe ihr script-Feld mit dem Pfad des eigenen Skripts, vom Holzfäller bis zum Heiler, die Konfiguration ist jetzt der Meldeschein und die Registry der Standesamt-Beamte, der nur noch nach dem Papier greift.
4. Ein neuer Beruf bedeutet ab sofort: Skript-Datei anlegen und Konfigurationszeile schreiben, die Registry selbst wird nicht mehr angerührt, ein Gasthaus, das Fremden Zimmer gibt, ohne die Hausordnung umzuschreiben.
5. Die berührten Dateien sind ausschließlich game/logic/kategorie_job/job_registry.gd und game/data/job_config.json, eine Klasse, ein Datenblatt, keine Verbraucher mussten mitziehen, denn die Außenfläche job_erzeugen bleibt unverändert.
6. Der volle Preflight zählt unverändert 130 Klassen auf 145 Dateien und läuft grün, der Warnungs-Scan nach Regel 6 bleibt ohne Befund, und die Rückwärtskompatibilität ist beweisbar, denn der Fallback deckt jeden früheren Weg ab.
7. Die sichtbare Ingame-Verifikation nach Regel 7 bleibt dem Editor-Lauf vorbehalten: Alle sechs Berufe müssen nach wie vor ihre Arbeit tun, das Kontextmenü muss die Jobliste unverändert zeigen, und ein flüchtiger Tipp im script-Feld müsste sichtbar eine Warnung werfen statt einen/gecrashten Job zu erzeugen.
