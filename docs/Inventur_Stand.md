# Inventur SnipWarfare (TASK 01)

Dieses Dokument ist die belastbare Arbeitsgrundlage für die nachfolgenden Tasks. Jeder Befund wurde am heutigen Code geprüft (Stand Commit 478ff72, 2026-09-10, 73 Commits seit der Initialisierung cf6736b, nachgezählt mit git rev-list --count). Nicht dokumentiert ist, was nicht am Code bestätigt wurde. Historische Fundlisten bleiben als Zeitdokumente stehen; ihre Aussagen gelten nur, wo dieser Text sie als heutig bestätigt.

## 1. Geprüfter Bestand

Klassen: 137 auf 154 GDScript-Dateien (Preflight-Ausgabe des letzten Laufs). JSON-Datenpools: 14 (core 3, game 6, world 5, population 3, economy 1; world zählt gebaeude.json, generator_gewichte.json, element_katalog.json, tier_verhalten.json, biome.json, welt_definition.json). SVG-Assets: 38. Szenen: 8 tscn. Autoloads: Weltuhr (Kern_Weltuhr), WeltSitzung (Ui_WeltSitzung), KernSignalBusAutoload (Kern_SignalBus). Laufbeweis: tools/lauf_pruefung_welt.gd mit 42 grünen Prüfungen. Preflight: 16 Prüfkategorien, Codes E001 bis E039, Voll-Lauf grün inklusive Headless-Godot und Warnungs-Scan.

## 2. Domänen und deren Zustand (heute bestätigt, Stand 2026-09-10, 23:38 Uhr)

Kern (core/): Weltuhr als einziger Tick und einzige Faktor-Übersetzung, Kern_Zufall als einzige Zufallsquelle, Kern_Mutation und Kern_Mutationsschema als Mutationsprotokoll, Kern_ModifikatorBasis/Registry/Maschine mit genau einer geteilten Registry-Instanz (Konsolidierung 478ff72), Kern_LogikBasis/Registry als markierte Plugin-Grenze ohne Verbraucher (Kopfkommentar bestätigt), Kern_SteuerungBasis/Registry als Steuerungsquelle, Pathfinding-Kette als lebendiges Modul mit Verbraucher (Einheit_WegPlanung nutzt Kern_PathNetz, Kern_PathFinder und Kern_PathRegistry, siehe Abschnitt 3), Kern_Timeline als Beobachtungs-Spitze mit HUD-Anschluss (warum-Knopf).

Welt (world/): Welt_Model als Datenbasis mit Regionen, Chunks und autoritativem Weltseed, Welt_Registry als Fassade über Terrain-, Natur- und Gebäude-Sichten mit genau einer Katalog-Ladung, Welt_Generator mit ortsfester Seed-Ableitung je Region und Chunk, Welt_DefinitionRegistry als Größen-Quelle, Welt_Speicher mit World- und Einzelwelt-Kompatibilität, Welt_World als Map-Anker, Welt_MapFabrik als einzige Kartenerzeugungsstelle (Expansion), Welt_Ladevorgang als einzige Laden-und-Erzeugen-Stelle, Tier-System (Registry, Status, Manager, Darsteller), Wärmefeld, Tageszyklus, Biom als Mutation, Objekt- und Gebäude-System (Bau- und Produktionsmaschine, Manager, DefinitionRegistry), Orchestrator (Registry, Konfiguration, Status, Manager, Darsteller, Verdrahtung), Renderer mit Objekt-Darstellungs-Spitze, Feedback-Anzeigen. Die Welt-Szene welt.gd ist reine Verdrahtung.

Game (game/): Einheit_Status als Arbeitsloop-Maschine mit eigener Queue und Wegplanung, Einheit_VitalStatus als Vital-Maschine, Einheit_Manager als Verbindungs- und Tick-Organisator, Ernte- und Versorgungs-Maschine als eigene Dateien, Ressourcen über Einheit_RessourcenSchema (Startbestände, Ernte mit Varianz) und Lager-Anbindung, Job_Registry mit script-Feld als Plugin-Grenze und sechs Jobs in job_config.json.

Population (population/): Pop_NeedBaum als struktureller Besitzer, Pop_NeedRegistry und Pop_RassenSchemaRegistry mit script-Feld-Plugin-Grenze, Pop_MoodMaschine je Einheit, Pop_MoodModifikatorRegistry als einzige Schwellwert-Quelle (Konsolidierung 478ff72), Pop_Denkblase als reine Beobachterin, Verteilungs-Dialog als isoliertes Fenster.

Economy (economy/): Lager_Registry, Lager_Manager, Lager_MutationEinlagern und Lager_MutationEntnehmen als Mutationen, verortete Bestände, globale Summe nur für HUD.

UI (ui/): Eingabe- und Kamera-Steuerung als Übersetzer, Panels als PackedScenes über Uebersetzer-Schnittpunkte, HUD-Beobachter, Kontextmenü aus steuerung.json, Hauptmenü mit World-Sitzung, Übergangsszene.

## 3. Klassifizierte Befunde

Bereits erledigt und am Code bestätigt: Weltuhr liegt in core/logic/clock/, Timeline in core/logic/events/, Weltwurzel-Dateien in kategorie_welt/, Ernte-Kette mit Absender-Bindung, Wärme-Gates lesen nur die Registry, Modifikator-Registry genau eine Ladung, tagezyklus hängt direkt an der Weltuhr, Räucherei als zweiter Datenpool-Beweis, World-Ebene und Expansion als Fabrik, Loop-Jobs für alle drei Ernte-Jobs, Wegplanung angeschlossen mit Cache und Pfad-Registry-Lesung.

Funktionierend und unverändert zu lassen: die gesamte Tick-Kette mit Wächtern und Disconnects, die Mutationssysteme (Ressourcen, Lager, Biom), die Gebäudekette über dieselbe Maschine, das Feedback über den Signalbus, die Renderer-Trennung von Tier-Darstellung, die Wegplanung mit Cache und gesperrter Zielfeld-Freigabe.

Offen und bewusst dokumentiert: Kern_LogikRegistry und Kern_LogikBasis ohne Verbraucher (Plugin-Grenze, Fundliste B-03, Kopfkommentar PLUGIN-GRENZE noch nicht aktiv ist am Code bestätigt), Welt_GrenzProfil ohne Verbraucher (Plugin-Grenze, Fundliste B-06, Kopfkommentar bestätigt), needs.json trägt kaelte_schwellwert, hitze_schwellwert, hp_abzug_je_tick_kaelte und hp_abzug_je_tick_hitze als lesende Beschreibung ohne direkten JSON-Leser an dieser Datei (die Wahrheit für die Maschinen liegt in mood_modifikatoren.json, gelesen über die Registry), tageszyklus-Werte 6/4/2 stehen in needs.json Zeile 16 und 17 als Beschreibung, werden aber an welt.gd Zeile 65 und tools/lauf_pruefung_welt.gd Zeile 582 hart übergeben, verbrauch_je_takt 0.8 liegt als Daten in needs.json Zeile 14 vor, wird aber in Einheit_Versorgung Zeile 10 als gleiches Default geführt.

Korrigiert gegen einen früheren Irrtum dieses Dokuments: Die Pathfinding-Kette ist NICHT tot. Einheit_WegPlanung (game/logic/kategorie_einheit/einheit_weg_planung.gd, Zeilen 13 bis 15) besitzt Kern_PathNetz, Kern_PathFinder und Kern_PathRegistry als eigene Bausteine, wird in Einheit_Manager Zeile 23 und 78 erzeugt und von tools/lauf_pruefung_welt.gd Zeile 618 im Beweislauf benutzt. Der Abschnitt pathfinding in steuerung.json wird von Kern_PathRegistry.laden gelesen, das Einheit_WegPlanung.netz_erneuern aufruft. Die Fundliste B-01, B-02 und C-02 beschrieb den Stand vor dem Anschluss und ist damit abgearbeitet.

Veraltet und nicht mehr im Code vorhanden: Kern_SteuerungUebersetzer (Dateiprüfung bestätigt: Datei existiert nicht mehr, Fundliste B-04), Orchestrator_Basis (Dateiprüfung bestätigt, Fundliste B-05), control.tscn (Dateiprüfung bestätigt, Fundliste F7), prototyp_karte (restlos entfernt), die früheren parallelen Erntepfade und die hart codierten Schwellwerte (Konsolidierung 478ff72), die toten Job-Felder benoetigt_werkzeug und koerperliche_anforderung (grep über job_config.json null Treffer, Fundliste C-04 und C-05), die gespiegelten Felder in Objekt_Kadaver und Objekt_Lagerfeuer (nur noch lagerfeuer_waerme_radius_kacheln und lagerfeuer_waerme_staerke als echte Wärme-Felder vorhanden, Fundliste E-02).

Doppelt, aber bewusst toleriert: Tier_Registry wird je Tier_Manager-Instanz geladen (Headless-Fähigkeit, nur lesend), Pop_NeedRegistry existiert im Need-Baum und im Manager (beide nur lesend, Wahrheit bleibt die Datei), mehrere Feedback-Anzeigen abonnieren die Weltuhr je Instanz (kurzlebig, sauber getrennt).

Fundlisten-Abarbeitungsstand am heutigen Code, je Punkt nachgeprüft: F-01 Job-Registry als Plugin-Grenze ist erledigt, job_config.json trägt script-Felder für alle sechs Jobs. F-02 Need-Registry als Plugin-Grenze ist erledigt, needs.json trägt script-Felder. F-03 Signal-Wrapper ist erledigt, alle vier ehemaligen Direktemittenten nutzen die Bus-Wrapper. A-01 Weltuhr-Move und A-02 Timeline-Move sind erledigt (Pfade bestätigt). B-03 und B-06 sind als Kopfdokumentation erledigt und bleiben als Plugin-Grenzen offen. B-04 und B-05 sind gelöscht. B-07 war widerlegt und bleibt lebendig. C-01 Warum-Knopf ist erledigt, C-02 ist durch den Wegplanungs-Anschluss überholt, C-03 bleibt wie beurteilt ohne Aktion. E-01 und E-02 sind erledigt. G UID-Begleiter ist fast erledigt: von einst 66 Skripten ohne Begleiter bleiben vier, die im Orphan-Sweep des Folge-Tasks geprüft werden.

Echte Architekturverletzung: keine gefunden. Die Registry- und Maschinenkette folgt dem Muster Daten, Registry, Maschine, Zustand, Darstellung an allen geprüften Stellen.

Echter Funktionsfehler am heutigen Code: keiner nachgewiesen. Die Pause-Menü-Rückkehr setzt get_tree().paused selbst zurück (Fundstelle F-I04 der Ingame-Liste ist damit am Code nicht mehr reproduzierbar), die MCP-Befunde F-I01 bis F-I03 liegen im Addon godot-acp, nicht im Spielcode.

Historisches Erbe ohne heutige Funktion: tools/kompilier_sweep.gd.uid ist ein verwaistes UID-Begleitfile ohne Skript (das Skript existiert nicht mehr). Es wird in diesem Task nicht entfernt, sondern nur dokumentiert; die Entfernung gehört in den Orphan-Sweep eines Folge-Tasks.

## 4. Historienzuordnung (je Behauptung am heutigen Code nachgeprüft)

Die 73 Commits seit cf6736b zeigen eine klare Linie: Aufbau Kern und Registries, Pathfinding und Generator, Lager und Orchestrator, Vital-Trennung und Signalbus, Wärme und Tageszyklus, Warnungs-Scan und Preflight-Härtung, Welt-Hierarchie mit Seed-Besitz, Produktionsökonomie mit zwei Datenpool-Ketten, World-Ebene und Expansion, Job-Queues, Manager-Verschmelzung zu Maschinen, Panel-Szenen, Verbindungs-Audit.

Abschlussprüfung der früheren offenen Punkte, jeder mit Beweis: Die Pathfinding-Kette galt in den Fundlisten als tot und ist heute am Code lebendig angeschlossen, Beweis sind die drei Bausteine in Einheit_WegPlanung und ihre Erzeugung im Einheit_Manager. Die Kern_LogikRegistry bleibt die einzige bewusst offene Plugin-Grenze des Kerns, Beweis ist der bestätigte Kopfkommentar und null Verbraucher. Die Wärme-Schwellwerte galten als doppelt geführt und sind konsolidiert, Beweis ist die Registry-Lesung in Pop_MoodMaschine und Einheit_VitalStatus. Die Ernte-Buchung galt als absenderlos und trägt heute die Absender-Bindung, Beweis ist der connect mit bind im Einheit_Manager. Die Pause-Menü-Rückkehr galt als Fehler und ist am Code widerlegt, sie setzt paused selbst zurück. Die Welt_DefinitionRegistry galt als verdächtig und ist als lebendig mit drei Verbrauchern bestätigt. Kein geprüfter Punkt bleibt unbewiesen offen; was offen bleibt, ist bewusst als Plugin-Grenze oder beschreibendes Datenfeld dokumentiert.

## 5. Arbeitsgrundlage für die Folge-Tasks

TASK 02 festigt die Datenkette und dokumentiert Besitz und Erweiterungsgrenze je Domäne. TASK 03 schließt das config-driven Inhaltsmodell (Tier- und Objekt-Zugänge sind bereits als script-Feld beziehungsweise zentrale Zuordnung vorhanden; Objekt-Seite hat noch keine Registrierungsschnittstelle, nur das match in Welt_Registry._objekt_klasse_fuer). TASK 04 prüft die sieben Stabilitätskandidaten am heutigen Code; mehrere sind durch frühere Slices bereits behoben (Indexverschiebung durch stabile Tier-IDs und Nachrücken, Signal-Matching durch Absender-Bindung, Akkumulator durch Rahmen-Budget). TASK 05 hält den Generator datengetrieben; Cluster existieren bereits in generator_gewichte.json. TASK 06 bis TASK 13 bauen entlang der bestehenden Domänen ohne Parallelen. TASK 14 erweitert den Laufbeweis um Verhaltensnachweise. TASK 15 räumt den Rest auf, inklusive des dokumentierten verwaisten UID-Begleitfiles.
